<?php
/**
 * Taxcloud_Magento2 - exemption certificates on the customer edit page.
 *
 * @var \Taxcloud\Magento2\Block\Adminhtml\Customer\Edit\Tab\Certificates $block
 * @var \Magento\Framework\Escaper $escaper
 */
if (!$block->canManageCertificates()) {
    return;
}
$options = $block->getFormOptions();
?>
<section class="admin__page-section taxcloud-certificates" data-taxcloud-certificates>
    <div class="admin__page-section-title">
        <span class="title"><?= $escaper->escapeHtml(__('TaxCloud Exemption Certificates')) ?></span>
    </div>

    <div class="admin__field-note">
        <?= $escaper->escapeHtml(__(
            'Certificates are held by TaxCloud, not by Magento. This panel shows what the account used by %1 holds for this customer.',
            $block->getStoreLabel()
        )) ?>
    </div>

    <div class="admin__field">
        <label class="admin__field-label"><?= $escaper->escapeHtml(__('Resolved TaxCloud customer ID')) ?></label>
        <div class="admin__field-control">
            <span data-role="identity">&mdash;</span>
            <span class="admin__field-note" data-role="identity-note"></span>
        </div>
    </div>

    <div class="admin__field-note" data-role="status"></div>

    <table class="admin__table-primary" data-role="certificate-table" style="display:none;">
        <thead>
        <tr>
            <th><?= $escaper->escapeHtml(__('Certificate')) ?></th>
            <th><?= $escaper->escapeHtml(__('Covers')) ?></th>
            <th><?= $escaper->escapeHtml(__('Purchaser')) ?></th>
            <th><?= $escaper->escapeHtml(__('Reason')) ?></th>
            <th><?= $escaper->escapeHtml(__('Actions')) ?></th>
        </tr>
        </thead>
        <tbody data-role="certificate-rows"></tbody>
    </table>

    <div class="admin__field" style="margin-top:1rem;">
        <button type="button" class="action-secondary" data-role="refresh">
            <?= $escaper->escapeHtml(__('Refresh from TaxCloud')) ?>
        </button>
        <button type="button" class="action-secondary" data-role="show-add">
            <?= $escaper->escapeHtml(__('Add Certificate')) ?>
        </button>
    </div>

    <fieldset class="admin__fieldset" data-role="add-form" style="display:none;">
        <legend class="admin__legend"><?= $escaper->escapeHtml(__('New exemption certificate')) ?></legend>
        <div class="admin__field-note">
            <?= $escaper->escapeHtml(__(
                'This certificate will apply to all of this customer\'s future orders in the states you choose. TaxCloud does not verify exemption claims — keep the signed certificate on file.'
            )) ?>
        </div>

        <div class="admin__field">
            <label class="admin__field-label"><?= $escaper->escapeHtml(__('States (comma separated)')) ?></label>
            <div class="admin__field-control"><input class="admin__control-text" data-field="states" placeholder="TX, NY"/></div>
        </div>
        <div class="admin__field">
            <label class="admin__field-label"><?= $escaper->escapeHtml(__('Purchaser name')) ?></label>
            <div class="admin__field-control">
                <input class="admin__control-text" data-field="firstName" placeholder="<?= $escaper->escapeHtmlAttr(__('First')) ?>"/>
                <input class="admin__control-text" data-field="lastName" placeholder="<?= $escaper->escapeHtmlAttr(__('Last')) ?>"/>
            </div>
        </div>
        <div class="admin__field">
            <label class="admin__field-label"><?= $escaper->escapeHtml(__('Address')) ?></label>
            <div class="admin__field-control">
                <input class="admin__control-text" data-field="address1" placeholder="<?= $escaper->escapeHtmlAttr(__('Street')) ?>"/>
                <input class="admin__control-text" data-field="city" placeholder="<?= $escaper->escapeHtmlAttr(__('City')) ?>"/>
                <input class="admin__control-text" data-field="state" placeholder="<?= $escaper->escapeHtmlAttr(__('State')) ?>"/>
                <input class="admin__control-text" data-field="zip" placeholder="<?= $escaper->escapeHtmlAttr(__('ZIP')) ?>"/>
            </div>
        </div>
        <div class="admin__field">
            <label class="admin__field-label"><?= $escaper->escapeHtml(__('Business type')) ?></label>
            <div class="admin__field-control">
                <select class="admin__control-select" data-field="businessType">
                    <?php foreach ($options['businessTypes'] as $type): ?>
                        <option value="<?= $escaper->escapeHtmlAttr($type) ?>"><?= $escaper->escapeHtml($type) ?></option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>
        <div class="admin__field">
            <label class="admin__field-label"><?= $escaper->escapeHtml(__('Reason')) ?></label>
            <div class="admin__field-control">
                <select class="admin__control-select" data-field="reason">
                    <?php foreach ($options['reasons'] as $reason): ?>
                        <option value="<?= $escaper->escapeHtmlAttr($reason) ?>"><?= $escaper->escapeHtml($reason) ?></option>
                    <?php endforeach; ?>
                </select>
                <input class="admin__control-text" data-field="reasonDescription"
                       maxlength="20"
                       placeholder="<?= $escaper->escapeHtmlAttr(__('Short description (max 20 chars)')) ?>"/>
            </div>
        </div>
        <div class="admin__field">
            <label class="admin__field-label"><?= $escaper->escapeHtml(__('Tax ID')) ?></label>
            <div class="admin__field-control">
                <input class="admin__control-text" data-field="taxId"/>
                <span class="admin__field-note">
                    <?= $escaper->escapeHtml(__('Recorded on V1 SOAP stores. The V3 REST API has no field for it and will not show it back.')) ?>
                </span>
            </div>
        </div>
        <div class="admin__field">
            <button type="button" class="action-primary" data-role="save"><?= $escaper->escapeHtml(__('Create Certificate')) ?></button>
            <button type="button" class="action-secondary" data-role="cancel"><?= $escaper->escapeHtml(__('Cancel')) ?></button>
        </div>
    </fieldset>
</section>

<script type="text/x-magento-init">
{
    "[data-taxcloud-certificates]": {
        "Taxcloud_Magento2/js/certificate/admin-certificates": <?= /* @noEscape */ $block->getJsConfig() ?>
    }
}
</script>
