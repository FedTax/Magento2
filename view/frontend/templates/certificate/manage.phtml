<?php
/**
 * Taxcloud_Magento2 - exemption certificates in My Account.
 *
 * @var \Taxcloud\Magento2\Block\Certificate\Manage $block
 * @var \Magento\Framework\Escaper $escaper
 */
?>
<div class="block taxcloud-certificates" data-taxcloud-certificates>
    <div class="block-title"><strong><?= $escaper->escapeHtml(__('Exemption Certificates')) ?></strong></div>

    <div class="block-content">
        <p>
            <?= $escaper->escapeHtml(__(
                'Certificates you add here are applied automatically at checkout when you ship to a state they cover.'
            )) ?>
        </p>

        <div data-role="status"></div>

        <table class="data table" data-role="certificate-table" style="display:none;">
            <caption class="table-caption"><?= $escaper->escapeHtml(__('Your exemption certificates')) ?></caption>
            <thead>
            <tr>
                <th scope="col" class="col"><?= $escaper->escapeHtml(__('Applies in')) ?></th>
                <th scope="col" class="col"><?= $escaper->escapeHtml(__('Issued to')) ?></th>
                <th scope="col" class="col"><?= $escaper->escapeHtml(__('Reason')) ?></th>
                <th scope="col" class="col"><?= $escaper->escapeHtml(__('Actions')) ?></th>
            </tr>
            </thead>
            <tbody data-role="certificate-rows"></tbody>
        </table>

        <div class="actions-toolbar" data-role="add-toolbar" style="display:none;">
            <div class="primary">
                <button type="button" class="action primary" data-role="show-add">
                    <span><?= $escaper->escapeHtml(__('Add a certificate')) ?></span>
                </button>
            </div>
        </div>

        <form class="form" data-role="add-form" style="display:none;">
            <fieldset class="fieldset">
                <legend class="legend"><span><?= $escaper->escapeHtml(__('Add an exemption certificate')) ?></span></legend>

                <div class="message notice">
                    <div>
                        <?= $escaper->escapeHtml(__(
                            'This certificate will apply to ALL of your future orders shipped to the states you choose, until you remove it. It is not a one-time exemption.'
                        )) ?>
                    </div>
                </div>

                <p class="note">
                    <?= $escaper->escapeHtml(__(
                        'You are responsible for knowing whether you qualify to claim exemption in these states. You may be liable for tax, interest and penalties if you are not eligible.'
                    )) ?>
                </p>

                <div class="field required">
                    <label class="label"><span><?= $escaper->escapeHtml(__('States this applies in')) ?></span></label>
                    <div class="control">
                        <input class="input-text" data-field="states" placeholder="TX, NY"/>
                        <span class="note"><?= $escaper->escapeHtml(__('Two-letter codes, separated by commas.')) ?></span>
                    </div>
                </div>

                <div class="field required">
                    <label class="label"><span><?= $escaper->escapeHtml(__('Your name')) ?></span></label>
                    <div class="control">
                        <input class="input-text" data-field="firstName" placeholder="<?= $escaper->escapeHtmlAttr(__('First name')) ?>"/>
                        <input class="input-text" data-field="lastName" placeholder="<?= $escaper->escapeHtmlAttr(__('Last name')) ?>"/>
                    </div>
                </div>

                <div class="field required">
                    <label class="label"><span><?= $escaper->escapeHtml(__('Address')) ?></span></label>
                    <div class="control">
                        <input class="input-text" data-field="address1" placeholder="<?= $escaper->escapeHtmlAttr(__('Street')) ?>"/>
                        <input class="input-text" data-field="city" placeholder="<?= $escaper->escapeHtmlAttr(__('City')) ?>"/>
                        <input class="input-text" data-field="state" placeholder="<?= $escaper->escapeHtmlAttr(__('State')) ?>"/>
                        <input class="input-text" data-field="zip" placeholder="<?= $escaper->escapeHtmlAttr(__('ZIP')) ?>"/>
                    </div>
                </div>

                <div class="field required">
                    <label class="label"><span><?= $escaper->escapeHtml(__('Type of business')) ?></span></label>
                    <div class="control">
                        <select class="select" data-field="businessType">
                            <?php foreach ($block->getBusinessTypes() as $type): ?>
                                <option value="<?= $escaper->escapeHtmlAttr($type) ?>"><?= $escaper->escapeHtml($type) ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                </div>

                <div class="field required">
                    <label class="label"><span><?= $escaper->escapeHtml(__('Reason for exemption')) ?></span></label>
                    <div class="control">
                        <select class="select" data-field="reason">
                            <?php foreach ($block->getReasons() as $reason): ?>
                                <option value="<?= $escaper->escapeHtmlAttr($reason) ?>"><?= $escaper->escapeHtml($reason) ?></option>
                            <?php endforeach; ?>
                        </select>
                        <input class="input-text" data-field="reasonDescription" maxlength="20"
                               placeholder="<?= $escaper->escapeHtmlAttr(__('Briefly, in 20 characters or fewer')) ?>"/>
                    </div>
                </div>

                <div class="field">
                    <label class="label"><span><?= $escaper->escapeHtml(__('Tax ID')) ?></span></label>
                    <div class="control"><input class="input-text" data-field="taxId"/></div>
                </div>
            </fieldset>

            <div class="actions-toolbar">
                <div class="primary">
                    <button type="button" class="action primary" data-role="save">
                        <span><?= $escaper->escapeHtml(__('Save certificate')) ?></span>
                    </button>
                </div>
                <div class="secondary">
                    <a class="action back" href="#" data-role="cancel"><span><?= $escaper->escapeHtml(__('Cancel')) ?></span></a>
                </div>
            </div>
        </form>
    </div>
</div>

<script type="text/x-magento-init">
{
    "[data-taxcloud-certificates]": {
        "Taxcloud_Magento2/js/certificate/my-account": <?= /* @noEscape */ $block->getJsConfig() ?>
    }
}
</script>
